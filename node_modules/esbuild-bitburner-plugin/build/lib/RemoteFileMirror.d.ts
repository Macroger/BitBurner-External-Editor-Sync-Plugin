import { FSWatcher } from 'chokidar';
import { RemoteApiServer } from './RemoteApiServer';
import { BitburnerPluginOptions } from '../types';
export declare class RemoteFileMirror {
    static remoteApi: RemoteApiServer;
    servers: string[];
    targetPath: string;
    fileCache: Record<string, string>;
    syncing: boolean;
    remotePollTimeout: NodeJS.Timeout | undefined;
    fileWatcher: FSWatcher | undefined;
    options: BitburnerPluginOptions;
    static create(targetPath: string, servers: NonNullable<BitburnerPluginOptions['mirror']>[string], options: BitburnerPluginOptions): Promise<RemoteFileMirror>;
    constructor();
    initFileCache(): Promise<void>;
    writeToFilesCache(files: Record<string, string>): void;
    compareFilesToCache(files: Record<string, string>): {
        mod: Record<string, string>;
        rem: Record<string, string>;
    };
    compareCacheToRemote(): Promise<{
        mod: Record<string, string>;
        rem: Record<string, string>;
    } | undefined>;
    getAllServerFiles(): Promise<Record<string, string> | undefined>;
    pushAllFiles(): Promise<void>;
    syncWithRemote(): Promise<void>;
    watch(): void;
    dispose(): void;
}
